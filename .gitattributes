# Arduino CLI emits canonical Intel HEX with CRLF records on every platform.
# Preserve those generated bytes exactly so CI's reproducibility check compares
# the flash artifact rather than Git's text normalization.
Arduino/build/**/*.hex binary
