# The Operator: idling in an era past the drift timeout pulls you back to the
# Operator (no progress lost).
activate_plugin The Operator
hook_up

keys 1955
wait 3
assert_display 1=LISTEN

# Touch nothing past the drift window
wait 31
assert_display DIAL A YEAR
assert_clip drift_back.wav
