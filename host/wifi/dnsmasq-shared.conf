# NetworkManager's shared-mode dnsmasq reads this directory. Resolve every
# hostname locally so captive-portal probes arrive at the setup page.
address=/#/10.42.0.1
dhcp-option=114,http://setup.millennium/
